# Dataset quality — what the scrape actually contained

Read this before any other document here. A pipeline that silently filters to the
interesting rows produces a report that looks identical whether the scrape worked
or failed, so the filtering is reported first and in full.

```
2037 rows in, 70 kept, 1967 removed (96.6%)
     35 duplicate rows
   1504 below the traction floor
    428 from bulk-generated accounts
        vmoebriiha                122 rows, no traction on any of them
        openclaw-mcp-vps          114 rows, no traction on any of them
        snighsbwang                99 rows, no traction on any of them
        designictics               41 rows, no traction on any of them
        x7yxpksk72-maker           31 rows, no traction on any of them
        lyw-xz                     21 rows, no traction on any of them
```

Every row is accounted for: 70 kept plus 1967 removed equals
2037 received. The filter raises rather than returning if that fails.

## Domain verdicts

| domain | rows | above floor | max stars | verdict |
|---|--:|--:|--:|---|
| AI Agent | 1409 | 68 | 8672 | **USABLE** |
| Long Context | 348 | 0 | 0 | **UNUSABLE** |
| RAG | 172 | 2 | 3588 | **THIN** |
| Multi-modal | 58 | 0 | 8 | **UNUSABLE** |
| Speculative Model | 50 | 0 | 1 | **UNUSABLE** |

Three of the five domain queries returned nothing usable. Their maxima are 0, 8 and 1
stars respectively — those are not thin results, they are failed queries, and every
conclusion in these documents is therefore scoped to **AI Agent** and **RAG** only.

## Why the noise rule is volume AND traction

An account with a hundred repositories is either a spammer or a major foundation.
Counting alone would delete the most credible publishers in the ecosystem, so an owner
is only removed when high volume comes with no traction anywhere in that volume.

## What survives

70 projects, holding 77957 of the
78480 stars in the file. The signal is 3.5% of the rows and
essentially all of the attention.
---

*Generated by `engine/scripts/scan_intel.py` from `intel/snapshots/metrics_20260515.csv` and `intel/evidence_20260805.json`. Every figure resolves to a record in the evidence file; the generator refuses to publish a document that cites anything else.*
